runtime: nodejs20

env: standard

instance_class: F1

env_variables:
  DB_HOST: "34.172.113.167"
  DB_USER: "admin"
  DB_PASSWORD: "mypassword"
  DB_NAME: "notes_123230189"
  PORT: "8080"

automatic_scaling:
  min_instances: 0
  max_instances: 2
  target_cpu_utilization: 0.65

handlers:
  - url: /.*
    script: auto
    secure: optional
